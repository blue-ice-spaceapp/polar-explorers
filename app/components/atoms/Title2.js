import styled from 'styled-components'
import { typography, themeColor } from '../../style/helpers'

const Title = styled.h2`
    font-family: Paytone One, sans-serif;
    color: ${ themeColor('primary.dark') };
`
export default Title
